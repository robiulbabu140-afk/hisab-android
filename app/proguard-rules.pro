# Add project specific ProGuard rules here.
# Minification is disabled for the release build in this Phase 1 build, so this file
# is currently unused, but is kept so `proguardFiles(...)` in app/build.gradle.kts resolves.
